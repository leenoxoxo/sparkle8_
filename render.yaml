services:
  - type: web
    runtime: static
    name: sparkle8
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
